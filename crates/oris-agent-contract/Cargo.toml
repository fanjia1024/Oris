[package]
name = "oris-agent-contract"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
publish = false
license = "MIT"
description = "External agent proposal contracts for Oris EvoKernel."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
